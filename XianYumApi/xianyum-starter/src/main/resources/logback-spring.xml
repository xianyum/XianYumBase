<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 定义日志的根目录 -->
    <springProperty scope="context" name="projectName" source="spring.application.name" defaultValue="BaseApi"/>
    <property name="LOG_HOME" value="logs"/>
    <property name="PROJECT_NAME" value="${projectName}"/>

    <property name="FILE_PATTEN" value="[%level] [%date{ISO8601}] [%logger] [%thread] [%X{traceId}] %msg%n"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>[%highlight(%-5level)] [%date{ISO8601}] [%boldGreen(%logger)] [%boldYellow(%thread)] [%magenta(%X{traceId})] %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ERROR 日志文件：只记录ERROR级别 -->
    <appender name="ERROR" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_HOME}/${PROJECT_NAME}/ERROR-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxHistory>10</maxHistory>
            <maxFileSize>100MB</maxFileSize>
            <!-- 可选：限制日志总大小，防止磁盘打满，推荐加上 -->
            <totalSizeCap>2GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>${FILE_PATTEN}</pattern>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
    </appender>

    <!-- INFO+DEBUG日志文件：原逻辑BUG修复：LevelFilter只匹配DEBUG，INFO/WARN会被DENY，改用ThresholdFilter -->
    <appender name="DEBUG_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_HOME}/${PROJECT_NAME}/INFO-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxHistory>10</maxHistory>
            <maxFileSize>100MB</maxFileSize>
            <totalSizeCap>2GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>${FILE_PATTEN}</pattern>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>DEBUG</level>
        </filter>
    </appender>

    <!-- 第三方包日志级别 -->
    <logger name="org.hibernate" level="WARN"/>
    <logger name="org.springframework" level="WARN"/>
    <logger name="org.quartz" level="WARN"/>
    <logger name="io.moquette" level="WARN"/>
    <logger name="org.springframework.context.support.PostProcessorRegistrationDelegate" level="ERROR"/>
    <logger name="io.undertow" level="WARN"/>
    <logger name="io.undertow.websockets.jsr" level="ERROR"/>
    <logger name="springfox.documentation" level="WARN"/>
    <logger name="org.xnio.nio" level="WARN"/>
    <logger name="com.geccocrawler.gecco" level="WARN"/>
    <logger name="org.reflections.Reflections" level="WARN"/>
    <logger name="cn.xianyum.extension.dao" level="DEBUG"/>

    <!-- dev/test/uat 环境 -->
    <springProfile name="dev,test,uat">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="ERROR"/>
            <appender-ref ref="DEBUG_FILE"/>
        </root>
    </springProfile>

    <!-- prod环境：pred,prod,tencent -->
    <springProfile name="pred,prod,tencent">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="ERROR"/>
            <appender-ref ref="DEBUG_FILE"/>
        </root>
    </springProfile>
</configuration>
